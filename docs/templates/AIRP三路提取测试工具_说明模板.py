"""AIRP 三路提取测试工具说明模板。

一、文件级总览
功能：用真实连续叙事验证 Event、Memory 和其他 Entity 能否在同一批次分别提取，
再由固定脚本形成引用闭合的候选网络。
架构位置：开发期实验入口，不是 Event/Memory 正文 Schema，不是 Resolver，也不直接
确认世界事实。历史分阶段工具仍用于对照，当前集成工具用于验证单次 Event 路线。
输入：酒馆 JSONL、批次大小、模型接口、人工 Event 边界样例，以及仅从环境变量读取
的 API 密钥。
输出：一份持续覆盖的 Markdown 记录，包含全部提示词、模型正式回复、候选、结构检查、
失败证据、最终网络和人工对照；不保存密钥或隐藏思维过程。

二、结构级说明
1. 客观 Event：一次回复同时给出可选边界和受影响 Event 内容；不要求每批切分。
2. 主观 Memory：只提取实际认知者的认知和来源，不重判 Event。
3. 其他 Entity：提取 Character、Location、Item、Organization、Skill、Concept 和
   Character Relation 候选，不生成正式 ID。
三项读取同一不可变快照，可以并发；完成后才由脚本分配正式测试 ID、绑定来源、补齐
亲历 Memory Owner/关键特写 Actor 的 Event 参与引用，并重建反向 Index。

三、失败与恢复
传输错误可以重试；机器结构最多要求模型定向修正一次，语义软误差只告警。任一必需
任务失败时不推进游标，成功候选按批次版本保留。恢复只补失败任务，不重新调用同一
快照中已成功的任务。结构网络通过只说明引用闭合，不代表边界、事实和实体匹配正确。
"""


def 说明入口() -> None:
    """真实入口见 ``tools/airp_extraction_probe.py``；本文件只供中文协作阅读。"""
