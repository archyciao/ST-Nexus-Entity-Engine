"""OpenCode Zen 关闭思考诊断工具说明模板。

文件级总览
==========
本工具用一个极短请求确认 Zen 是否实际关闭隐藏推理，只用于接口诊断，不判断 Event 质量。

架构位置
========
它复用 Event 测试器的流式传输代码，位于正式语义测试之前。换模型、换端点或发现异常长
推理时先运行它，可以避免把接口参数问题误判成提示词问题。

输入
====
- Zen Chat Completions 端点与模型名；
- 环境变量中的 API 密钥；
- 总超时、无流量超时、正文启动超时和极小输出上限。

流程
====
1. 发送固定的“只输出 OK”提示，不携带游戏正文。
2. 同时发送官方关闭字段和 Zen 实测兼容字段。
3. 分别统计隐藏推理与正式正文字数，不保存隐藏推理原文。
4. 按实际响应判断关闭成功、未遵从或无法判断。

输出与边界
==========
输出只包含非敏感请求控制项、正式正文、时延、结束原因和字符数，不包含密钥。一次成功只
证明该次路由遵从参数；正式运行仍须保留流式监测。本文件只供中文协作阅读，不参与运行。
"""


# 三种结果供界面和测试记录使用，不代表模型能力评价。
VERDICT_EXAMPLE = {
    "thinking_disabled_observed": "收到正式正文，隐藏推理为零",
    "thinking_disable_not_honored": "仍收到隐藏推理",
    "inconclusive": "没有足够响应判断",
}
