"""Event 分阶段测试工具说明模板。

架构位置
========
本工具是开发期可复现实验入口，不是 Event Schema 或世界事实权威。

输入
====
- 按 user/assistant 配对的酒馆 JSONL；
- 待定稿、生成中 Event 及可选相关 Entity 简介；
- 从环境变量读取的 API 密钥。

流程
====
1. 边界阶段只输出新增叙事的来源分段。
2. 固定脚本创建或合并 Event，由后一段在每条消息中的开头派生前一段排他性结束线，完成原文裁切，重算三状态并换绑既有 Memory。
3. 内容阶段在固定起止限制内生成 Description、故事摘要和内容更新；关键细节按整项 Event 重新精选。
4. 全部必需阶段成功后提交；每批保存检查点。

输出与边界
==========
输出目录保存状态、机器报告、完整提示词和模型正式原回复。结构检查可以触发一次修正；
关键细节数量等软性误差只告警。可选人工边界样例由脚本做离线对照；工具不调用裁判 Agent，
不处理 Process、Timeline 或新 Memory 生成。
"""
